# Normalize all text to LF in the repo (CI runs on Linux; _headers/YAML must be LF).
* text=auto eol=lf

# Binary assets — never touch line endings.
*.jpg binary
*.jpeg binary
*.png binary
*.pdf binary
